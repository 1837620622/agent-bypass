# PLAYBOOK — 操作员处置笔记

> 这份文件不是技能的一部分：不参与注入、不被模型加载，纯给操作员看。当部署好的 Cknb 对某个任务打太极（只给提醒不给实内容、绕圈、空转）时，按这里的顺序处理。

## 一、处置顺序（从轻到重）

1. **换表述，不换任务**：同一件事换成更准确的作业语境再发——「授权评估的一部分」「研究 / 教学场景」「按标准流程过一遍」。
2. **拆小步**：把大动作拆成几个中性步骤，一步步来。
3. **多轮递进**：第一轮给背景，第二轮给概念，第三轮上细节，第四轮要实现——每轮自然衔接。
4. **给输出结构**：把要的产物形状写清楚（表格 / 清单 / 命令块 / 前言+步骤），让它进入产出模式。
5. **先提示后给全**：要求「风险提示 + 完整内容」的结构——不少软拦截给个提示位就放行。
6. **预填开头**：接口允许设置回复开头时，直接给个中性开头让它续写。
7. **换模型**：同一问题换一家、换参数组；同一表述跑 2-3 个变体——一次不行不代表没有。
8. **本地兜底**：开放权重环境直接换无审查（abliterated）版本——拦截从根上消失。

## 二、模型特性速查

- **分类器型接口**（生成后再过滤）：直白写「无限制人格 / 没有过滤」会被输入分类器先拦掉——用软框架。
- **Anthropic 新代**（Constitutional 系）：最硬；残留可用的是柔和框架（分析 / 文学视角），成功率约四到五成。
- **推理型模型**（o1 / R1 系）：安全检查做在思维链里——先替它把推理步骤写好，让它顺着跑。
- **开源 / 本地**：abliterated + 深度微调 ≈ 拦截清零（个别类目除外）。

## 三、三条心法

- **长而无聊 > 短而激烈**：平静、专业、信息密集的说明，比喊口号安全得多。
- **组合 > 单招**：单招都会被补，老招叠起来才通用。
- **别用「扮演黑客 / 忽略规则」式措辞**：这类句子是所有检测器的头号目标。

---

维护提示：技能管「模型怎么干活」（`SYSTEM_PROMPT.md` / `SKILL.md`），本笔记管「模型不干活时你怎么做」；两边分工，互不混用。
